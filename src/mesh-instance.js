class MeshInstance
{
    constructor(mesh, position, rotation, scale)
    {
        this.mesh = mesh;
        this.position = position;
        this.rotation = rotation;
        this.scale = scale;
    }
}